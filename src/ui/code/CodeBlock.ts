import LanguageButton from "./LanguageButton.ts";

export default class CodeBlock {

  protected container: Element

  protected constructor(container: Element) {
    this.container = container

  }

  static setup(node: Node) {

  }
}