// const current = this.messageBox.find(key)
// current.orElse(() => {
//   console.debug("except")
// })
//
// current.ifPresent(() => {
//
//   current.find("progress")
//     .orElse(() => {
//       console.debug("except")
//     })
// })

// if (![MessageBoxMode.Simple, MessageBoxMode.Detail].includes(m => m == this.messageBox.mode)) {
//   console.debug("except")
// }