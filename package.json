{
  "name": "workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "publishConfig": {
    "publish": false
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "http-server -p 3080 --cors -c-1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "http-server": "^14.1.1",
    "lerna": "^6.6.1"
  }
}
